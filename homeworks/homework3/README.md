# Домашно 3

В това домашно ще създадем Python SDK за [The Movie Database (TMDb) API](https://developers.themoviedb.org/3).

## SDKs

SDK (Software Development Kit) е колекция от инструменти, библиотеки и документация, които улесняват разработчиците в създаването на приложения за определена платформа или услуга. SDK предоставя готови за използване компоненти, които спестяват време и усилия при разработката.

Честа практика е популярни REST API-та да предоставят официални SDK-та за различни програмни езици, които улесняват разработчиците при интеграцията с техните услуги.

В това домашно ще създадем Python SDK за част от функционалностите на TMDb API, понеже такава официална библиотека липсва. Ще упражним знанията за работа с REST API-та, обработка на JSON данни, четене на документация, работа с пакети и модули, и спазване на добрите питонски практики. 🐍

## Демо

В [main.py](./main.py) можете да разгледате (и използвате за разработка) примерен код, който използва SDK-то, което трябва да създадете. След като напишете SDK-то, стартирането на `main.py` трябва да изведе информация за популярни филми, филми от 2025-та година с най-висок рейтинг, жанрове, комедийни екшън филми и популярни сериали.

## Преди да започнете

Издайте си [безплатен API ключ от TMDb](https://www.themoviedb.org/settings/api). Можете да използвате [python-dotenv](https://pypi.org/project/python-dotenv/) за зареждане на ключа под формата на променлива на средата (env var) от `.env` файл, или просто да си го задавате ръчно в сесията на терминала чрез `export TMDB_API_KEY="your_api_key"` (Linux/MacOS) или `set TMDB_API_KEY="your_api_key"` (Windows).

⚠️ Не ни качвайте API ключа в архива.

## Изисквания

### Интерфейс
Вашата библиотека трябва да бъде под формата на пакет с име `tmdb_sdk`, който съдържа следните публични класове и методи със следния очакван интерфейс (*обърнете внимание и на модулите/подпакетите, в които трябва да се намират*):

* Клас `tmdb_sdk.client.TMDBClient`:

  * Инициализатор, който приема `api_key: str` (въпросният API ключ за достъп) (при липсва на такъв, да се хвърли `ValueError` с точното съобщение `API key is required`).

  * Метод за най-популярни филми (заявка към [`GET /discover/movie`](https://developers.themoviedb.org/3/discover/movie)):
    ```python
    def discover_movies(
        self,
        language: str | None = None,
        region: str | None = None,
        sort_by: str | None = None,
        year: int | None = None,
        genres: Genre | GenreQuery | None = None,
        limit: int = 10,
    ) -> list[Movie]
    ```

  * Метод за най-популярни сериали (заявка към [`GET /discover/tv`](https://developers.themoviedb.org/3/discover/tv-discover)):
    ```python
    def discover_series(
        self,
        language: str | None = None,
        sort_by: str | None = None,
        genres: Genre | GenreQuery | None = None,
        limit: int = 10,
    ) -> list[Series]
    ```

  * Метод за всички жанрове на филми (заявка към [`GET /genre/movie/list`](https://developers.themoviedb.org/3/genres/get-movie-list)):
    ```python
    def get_movie_genres(
        self,
        language: str | None = None,
    ) -> list[Genre]
    ```

  * Метод за всички жанрове на сериали (заявка към [`GET /genre/tv/list`](https://developers.themoviedb.org/3/genres/get-tv-list)):
    ```python
    def get_series_genres(
        self,
        language: str | None = None,
    ) -> list[Genre]
    ```

  * Метод за търсене на филми (заявка към [`GET /search/movie`](https://developers.themoviedb.org/3/search/search-movies)):
    ```python
    def search_movies(
        self,
        query: str,
        language: str | None = None,
        year: int | None = None,
        limit: int = 10,
    ) -> list[Movie]
    ```

  * Метод за търсене на сериали (заявка към [`GET /search/tv`](https://developers.themoviedb.org/3/search/search-tv-shows)):
    ```python
    def search_series(
        self,
        query: str,
        language: str | None = None,
        limit: int = 10,
    ) -> list[Series]
    ```

* Клас `tmdb_sdk.models.Movie` с публични атрибути:
    ```python
    id: int
    title: str
    vote_average: float = 0.0
    original_title: str | None = None
    overview: str | None = None
    release_date: str | None = None
    poster_path: str | None = None
    backdrop_path: str | None = None
    genre_ids: list[int] | None = None
    popularity: float | None = None
    vote_count: int | None = None
    adult: bool | None = None
    original_language: str | None = None
    video: bool | None = None
    ```

* Клас `tmdb_sdk.models.Series` с публични атрибути:
    ```python
    id: int
    name: str
    vote_average: float = 0.0
    original_name: str | None = None
    overview: str | None = None
    first_air_date: str | None = None
    poster_path: str | None = None
    backdrop_path: str | None = None
    genre_ids: list[int] | None = None
    popularity: float | None = None
    vote_count: int | None = None
    original_language: str | None = None
    ```

* Клас `tmdb_sdk.models.Genre` с публични атрибути:
    ```python
    id: int
    name: str
    ```

* Клас `tmdb_sdk.models.GenreQuery`, който предефинира `__str__` метода по подходящ начин, и позволява създаването на жанрови заявки чрез логическите оператори AND и OR. *Повече за това в следваща секция.*

* Класове за изключения в `tmdb_sdk.exceptions`:
  * `TMDBException` - базов клас за всички ваши изключения в SDK-то.
  * `TMDBAuthenticationError` - за грешки с код 401, свързани с автентикация (напр. невалиден API ключ).
  * `TMDBNotFoundError` - за грешки с код 404, когато даден ресурс не е намерен.
  * `TMDBRateLimitError` - за грешки с код 429, когато е достигнат лимитът на заявки от нас.
  * `TMDBServerError` - за грешки с код 5xx, когато има проблем със сървъра на TMDB.
  
  *⚠️ Не използвайте `raise_for_status()`, а проверявайте директно статус кода на върнатия от сървъра отговор.*

### Параметъра `limit`

Неговата логика не е вградена в TMDb API, а трябва да я имплементирате вие. Той определя максималния брой резултати, които методът трябва да върне.

TMDb API използва странициране (pagination) с по определен брой резултати на страница. Ако `limit` е по-голям от размера на една страница, трябва библиотеката да продължава да заявки, докато не изчепра необходимия брой резултати (`<=limit`).

За да разберете как да използвате страницирането, вижте в документацията на TMDb API полетата на заявките и на отговорите, свързани с параметъра `page`. Тя е еднаква за всички техни ендпойнти. Общо взето:

- Подавате в заявката параметър `page`, който указва коя страница искате да получите (първата е `1`).
- В отговора има поле `total_pages`, което ви казва колко страници да очаквате общо

При невалдина стойност за `limit`, библиотеката да хвърли `ValueError`.

### Жанрови заявки

Полето за жанр е едно от тези, които поддържат по-сложно филтриране чрез логическите операции конюнкция (**AND**) и дезюнкция (**OR**), по следния начин:

  - За да укажем в заявките към TMDB API че искаме филмите/сериалите да имат *едновременно* жанр с ID 42 И жанр с ID 67, пращаме `with_genres=42,67` (логическа операция **AND**).
  - За да укажем в заявките към TMDB API че искаме филмите/сериалите да имат жанр с ID 42 ИЛИ жанр с ID 67, пращаме `with_genres=42|67` (логическа операция **OR**).

За да удобно моделиране на тази функционалност, ще предефинирате побитовите логически оператори `&` (**AND**) и `|` (**OR**) в класoвете `GenreQuery` и `Genre`, така че да може да се комбинират по следните примерни начини:

```python
comedy = Genre(id=35, name="Comedy")
action = Genre(id=28, name="Action")
drama = Genre(id=18, name="Drama")

str(comedy & action) == "35,28"  # Комедия И Екшън
str(comedy | action) == "35|28"  # Комедия ИЛИ Екшън
str(comedy & action & drama) == "35,28,18"  # Комедия И Екшън И Драма
# и т.н.
```

При невалиден тип на стойност за параметрите `genres` на методите на `TMDBClient` (т.е. типове, различни от `Genre`, `GenreQuery` или `None`), да се хвърли `TypeError`.

### Други задължителни изисквания

⚠️ Задължително трябва да използвате `requests.Session` обект за изпращане на HTTP заявките към TMDb API.


### Type hints

За да получите максималния брой точки, типовете на аргументите на функциите ви и това какво връща функцията трябва да бъдат указани чрез type hints.

### Структура

В решението ви очакваме `pyproject.toml` файл, който указва зависимостите (dependencies), както и къде се намира основният пакет `tmdb_sdk` (ако има нужда от настройки, различни от тези по подразбиране, примерно при наличие на `src` обграждаща директория).

### Добри практики

Очакваме да спазвате добрите питонски практики и конвенции като PEP-8. Можем да ви предоставим [pylint](https://pypi.org/project/pylint/) конфигурационен файл, който да използвате за проверка на стила на кода.


